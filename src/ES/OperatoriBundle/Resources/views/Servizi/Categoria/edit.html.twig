{% extends '::base.html.twig' %}

{% block title %}
Modifica Categoria Servizi
{% endblock %}

{% block content %}
<h1>Modifica Categoria Servizi</h1>

<form action="{{ path('admin_categorie_servizi_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_categorie_servizi') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('admin_categorie_servizi_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
