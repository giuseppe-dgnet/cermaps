{% extends '::base.html.twig' %}

{% block title %}
Nuova Categoria Servizi
{% endblock %}

{% block content %}
<h1>Nuova Categoria Servizi</h1>

<form action="{{ path('admin_categorie_servizi_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_categorie_servizi') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
